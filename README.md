# 📱 Max Notify для Home Assistant

Кастомная интеграция Home Assistant для отправки текстовых уведомлений в мессенджер **Max** через официальный API ([platform-api.max.ru](https://dev.max.ru/docs-api)).

---

## 📑 Содержание

- [Возможности](#-возможности)
- [Требования](#-требования)
- [Установка](#-установка)
- [Настройка](#-настройка)
- [Использование](#-использование)
- [Получение токена и идентификаторов](#-получение-токена-и-идентификаторов)
- [Структура репозитория](#-структура-репозитория)
- [Ссылки](#-ссылки)

---

## ✨ Возможности

- **Отправка текстовых уведомлений** в личку (`user_id`) или в групповой чат (`chat_id`)
- **Настройка через UI** (Config Flow): токен бота, тип получателя, ID — без правки YAML
- **Использование в автоматизациях и сценариях** через сущность notify

---

## 📋 Требования

- **Home Assistant** (актуальная версия)
- **Бот в мессенджере Max** и токен из раздела «Интеграция» платформы [MAX для разработчиков](https://dev.max.ru/docs-api)

---

## 🚀 Установка

### Установка через HACS (рекомендуется)

1. Убедитесь, что у вас установлен [HACS](https://hacs.xyz/) (Home Assistant Community Store).
2. Откройте **HACS** → **Интеграции** → меню (три точки) → **Добавить репозиторий**.
3. В поле **Репозиторий** введите: `https://github.com/kai-zer-ru/max-notyfy-ha`.
4. В поле **Категория** выберите **Интеграция** → нажмите **Добавить**.
5. В списке интеграций найдите **Max Notify** и нажмите **Установить**.
6. Перезапустите Home Assistant.
7. Добавьте интеграцию через **Настройки** → **Устройства и службы** → **Добавить интеграцию** → **Max Notify**.

> 💡 После добавления репозитория в HACS обновления интеграции можно ставить через HACS (появится кнопка обновления при выходе новых версий).

### Ручная установка

1. Склонируйте репозиторий или скачайте архив и скопируйте папку `custom_components/max_notify` в каталог конфигурации Home Assistant:

   ```bash
   # Пример: копирование в config/custom_components/
   cp -r custom_components/max_notify /path/to/homeassistant/config/custom_components/
   ```

2. Перезапустите Home Assistant.

---

## ⚙️ Настройка

1. Откройте **Настройки** → **Устройства и службы** → **Добавить интеграцию**
2. Найдите **Max Notify** и выберите его
3. **Шаг 1:** введите токен доступа бота (интеграция проверит его через API)
4. **Шаг 2:** выберите тип получателя:
   - **Личное сообщение** — укажите **User ID**
   - **Групповой чат** — укажите **Chat ID**
5. После сохранения появится сущность вида `notify.max_user_123` или `notify.max_chat_456`

---

## 📤 Использование

### В автоматизации

**Действие** → **Уведомление** → выберите сущность Max Notify → укажите сообщение и при необходимости заголовок.

### Вызов сервиса (YAML)

```yaml
service: notify.send_message
data:
  entity_id: notify.max_user_123
  message: "Текст уведомления"
  title: "Заголовок"
```

---

## 🔑 Получение токена и идентификаторов

### 🔐 Токен бота (access token)

- Создайте бота на платформе [MAX для разработчиков](https://dev.max.ru/docs-api) (dev.max.ru)
- Токен выдаётся при создании бота
- **Где взять:** платформа MAX для партнёров → раздел **Интеграция** → **Получить токен**
- Токен передаётся только в заголовке `Authorization`; не передавайте его в URL
- Не разглашайте токен — его могут отозвать за нарушение правил платформы

> ⚠️ **Важно:** храните токен в секрете. В Home Assistant он сохраняется в конфигурации интеграции.

### 👤 User ID (для личных сообщений)

- Числовой идентификатор пользователя или бота в MAX (int64)
- **У бота:** вызовите `GET https://platform-api.max.ru/me` с заголовком `Authorization: <токен>` — в ответе будет поле `user_id`
- **User ID нужного пользователя** возвращается в ответах API (объект [User](https://dev.max.ru/docs-api/objects/User) в методах работы с чатами, участниками, обновлениями и т.д.)
- Для теста можно использовать свой `user_id` (например, из ответа `/me` другого бота или из профиля)
- **Важно:** бот может писать в личку только тем, кто **первым начал диалог** с ботом в Max (нажал «Начать» или отправил любое сообщение боту). Иначе API может вернуть 403. Для проверки интеграции удобно использовать групповой чат (`chat_id`).

**Пример запроса для проверки токена и получения user_id бота:**

```bash
curl -X GET "https://platform-api.max.ru/me" \
  -H "Authorization: ВАШ_ТОКЕН"
```

### 💬 Chat ID (для групповых чатов)

- Числовой идентификатор группового чата (int64)
- **Как получить:** запрос `GET https://platform-api.max.ru/chats` с заголовком `Authorization: <токен>`
- В ответе — массив `chats`; в каждом объекте [Chat](https://dev.max.ru/docs-api/objects/Chat) есть поле `chat_id`
- Бот должен быть **добавлен в групповой чат** в мессенджере Max — тогда этот чат появится в списке

**Пример запроса списка чатов:**

```bash
curl -X GET "https://platform-api.max.ru/chats" \
  -H "Authorization: ВАШ_ТОКЕН"
```

> 💡 **Подсказка:** для программного получения ID смотрите официальную документацию: [GET /me](https://dev.max.ru/docs-api/methods/GET/me), [GET /chats](https://dev.max.ru/docs-api/methods/GET/chats), объекты [User](https://dev.max.ru/docs-api/objects/User) и [Chat](https://dev.max.ru/docs-api/objects/Chat).

---

## 📁 Структура репозитория

```
max-notyfy-ha/
├── hacs.json              # манифест для установки через HACS
├── README.md
└── custom_components/
    └── max_notify/
        ├── __init__.py
        ├── config_flow.py
        ├── const.py
        ├── manifest.json
        ├── notify.py
        ├── strings.json
        └── translations/
            ├── en.json
            └── ru.json
```

---

## 🔗 Ссылки

- [Репозиторий интеграции](https://github.com/kai-zer-ru/max-notyfy-ha) — исходный код на GitHub
- [Документация API Max](https://dev.max.ru/docs-api) — официальная документация для разработчиков
- [MAX для разработчиков](https://dev.max.ru/) — платформа для создания ботов
